shader_type canvas_item;

uniform vec2 cutoffs[5]

uniform float offset_multipliers[5]

void fragment() {
	if (y < sum_slices) {
		sum_slices += cutoffs[i][0] + sin(TIME * offset_multipliers[i];
		running_b += cutoffs[i][1] + sin(TIME * offset_multipiers[i];
	}
	else {
		break;
	}
}
